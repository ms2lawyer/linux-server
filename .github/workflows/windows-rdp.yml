name: Windows Cloud RDP

on:
  schedule:
    # GitHub Actions cron does not support every 2 minutes.
    # */5 * * * * = every 5 minutes.
    - cron: '*/5 * * * *'

  workflow_dispatch:

jobs:
  rdp-server:

    runs-on: windows-latest

    # The job itself takes more than 2 minutes because of:
    # checkout + cache restore + RDP setup + Tailscale setup
    # + 2 minute session + Desktop backup + cache save.
    timeout-minutes: 15

    permissions:
      contents: read
      actions: write

    steps:

      # ============================================================
      # 1. Checkout Repository
      # ============================================================
      - name: 1. Checkout Repository
        uses: actions/checkout@v5


      # ============================================================
      # 2. Restore Previous Persistent Workspace
      # ============================================================
      - name: 2. Restore Previous Workspace
        id: restore_workspace
        uses: actions/cache/restore@v5
        with:
          path: ${{ github.workspace }}\workspace

          # Every run gets a new cache key.
          #
          # GitHub Actions cache entries are immutable.
          key: cynet-rdp-workspace-${{ runner.os }}-${{ github.run_id }}

          # Restore the newest previous successful workspace.
          restore-keys: |
            cynet-rdp-workspace-${{ runner.os }}-


      # ============================================================
      # 3. Prepare Persistent Workspace
      # ============================================================
      - name: 3. Prepare Persistent Workspace
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'

          $Workspace = Join-Path $env:GITHUB_WORKSPACE 'workspace'
          $DesktopBackup = Join-Path $Workspace 'Desktop'

          Write-Host ""
          Write-Host "==================================================" -ForegroundColor Cyan
          Write-Host "PERSISTENT WORKSPACE"
          Write-Host "==================================================" -ForegroundColor Cyan
          Write-Host "Workspace     : $Workspace"
          Write-Host "Desktop Backup: $DesktopBackup"
          Write-Host ""

          if (-not (Test-Path -LiteralPath $Workspace)) {
              New-Item `
                -ItemType Directory `
                -Path $Workspace `
                -Force | Out-Null
          }

          if (-not (Test-Path -LiteralPath $DesktopBackup)) {
              New-Item `
                -ItemType Directory `
                -Path $DesktopBackup `
                -Force | Out-Null
          }

          Write-Host "Restore information:"
          Write-Host "Exact cache hit : ${{ steps.restore_workspace.outputs.cache-hit }}"
          Write-Host "Matched cache   : ${{ steps.restore_workspace.outputs.cache-matched-key }}"
          Write-Host ""

          Write-Host "Restored workspace:"
          Write-Host "--------------------------------------------------"

          Get-ChildItem `
            -LiteralPath $Workspace `
            -Force `
            -Recurse `
            -ErrorAction SilentlyContinue |
            Select-Object FullName, Length, LastWriteTime |
            Format-Table -AutoSize

          Write-Host "--------------------------------------------------"


      # ============================================================
      # 4. Enable Remote Desktop
      # ============================================================
      - name: 4. Enable Remote Desktop
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'

          Write-Host "Enabling Remote Desktop..."

          Set-ItemProperty `
            -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
            -Name 'fDenyTSConnections' `
            -Value 0

          # Keep Network Level Authentication enabled.
          Set-ItemProperty `
            -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
            -Name 'UserAuthentication' `
            -Value 1

          Enable-NetFirewallRule `
            -DisplayGroup 'Remote Desktop'

          Set-Service `
            -Name 'TermService' `
            -StartupType Automatic

          Start-Service `
            -Name 'TermService' `
            -ErrorAction SilentlyContinue

          Start-Sleep -Seconds 2

          $Service = Get-Service -Name 'TermService'

          Write-Host "TermService status: $($Service.Status)"

          if ($Service.Status -ne 'Running') {
              throw "Remote Desktop service is not running."
          }

          Write-Host "Remote Desktop enabled successfully."


      # ============================================================
      # 5. Create RDP Administrator Account
      # ============================================================
      - name: 5. Create RDP Administrator Account
        shell: powershell
        env:
          RDP_PASSWORD: ${{ secrets.RDP_PASSWORD }}
        run: |
          $ErrorActionPreference = 'Stop'

          $Username = 'CynetAdmin'

          if ([string]::IsNullOrWhiteSpace($env:RDP_PASSWORD)) {
              throw "GitHub Secret RDP_PASSWORD is not configured."
          }

          $SecurePassword = ConvertTo-SecureString `
            $env:RDP_PASSWORD `
            -AsPlainText `
            -Force

          Write-Host "Configuring user: $Username"

          $ExistingUser = Get-LocalUser `
            -Name $Username `
            -ErrorAction SilentlyContinue

          if ($null -eq $ExistingUser) {

              Write-Host "Creating local user..."

              New-LocalUser `
                -Name $Username `
                -Password $SecurePassword `
                -PasswordNeverExpires `
                -AccountNeverExpires `
                -Description 'GitHub Actions RDP Administrator' |
                Out-Null
          }
          else {

              Write-Host "User already exists."

              Set-LocalUser `
                -Name $Username `
                -Password $SecurePassword

              Enable-LocalUser `
                -Name $Username
          }

          Add-LocalGroupMember `
            -Group 'Administrators' `
            -Member $Username `
            -ErrorAction SilentlyContinue

          Add-LocalGroupMember `
            -Group 'Remote Desktop Users' `
            -Member $Username `
            -ErrorAction SilentlyContinue

          Write-Host "RDP user configured successfully."


      # ============================================================
      # 6. Restore Desktop From Persistent Workspace
      # ============================================================
      - name: 6. Restore CynetAdmin Desktop
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'

          $Username = 'CynetAdmin'

          $UserProfile = Join-Path 'C:\Users' $Username
          $Desktop = Join-Path $UserProfile 'Desktop'

          $Workspace = Join-Path $env:GITHUB_WORKSPACE 'workspace'
          $DesktopBackup = Join-Path $Workspace 'Desktop'

          Write-Host ""
          Write-Host "==================================================" -ForegroundColor Cyan
          Write-Host "RESTORING CynetAdmin DESKTOP"
          Write-Host "==================================================" -ForegroundColor Cyan
          Write-Host "Source : $DesktopBackup"
          Write-Host "Target : $Desktop"
          Write-Host ""

          if (-not (Test-Path -LiteralPath $UserProfile)) {
              New-Item `
                -ItemType Directory `
                -Path $UserProfile `
                -Force | Out-Null
          }

          if (-not (Test-Path -LiteralPath $Desktop)) {
              New-Item `
                -ItemType Directory `
                -Path $Desktop `
                -Force | Out-Null
          }

          if (-not (Test-Path -LiteralPath $DesktopBackup)) {
              New-Item `
                -ItemType Directory `
                -Path $DesktopBackup `
                -Force | Out-Null
          }

          # Restore saved Desktop.
          #
          # /MIR:
          # Makes the live Desktop match the saved Desktop.
          #
          # /COPY:DAT:
          # Copies Data, Attributes and Timestamps.
          #
          # /DCOPY:DAT:
          # Copies directory data, attributes and timestamps.
          #
          # /XJ:
          # Prevents junction loops.
          robocopy `
            $DesktopBackup `
            $Desktop `
            /MIR `
            /COPY:DAT `
            /DCOPY:DAT `
            /R:2 `
            /W:1 `
            /XJ

          # Robocopy exit codes 0-7 mean success/non-fatal.
          if ($LASTEXITCODE -gt 7) {
              throw "Desktop restore failed. Robocopy exit code: $LASTEXITCODE"
          }

          Write-Host ""
          Write-Host "Desktop restore completed."

          Write-Host ""
          Write-Host "Current Desktop contents:"
          Write-Host "--------------------------------------------------"

          Get-ChildItem `
            -LiteralPath $Desktop `
            -Force `
            -Recurse `
            -ErrorAction SilentlyContinue |
            Select-Object FullName, Length, LastWriteTime |
            Format-Table -AutoSize

          Write-Host "--------------------------------------------------"


      # ============================================================
      # 7. Install Tailscale
      # ============================================================
      - name: 7. Install Tailscale
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'

          $Installer = 'C:\tailscale.msi'
          $Tailscale = 'C:\Program Files\Tailscale\tailscale.exe'

          Write-Host "Downloading Tailscale..."

          Invoke-WebRequest `
            -Uri 'https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi' `
            -OutFile $Installer

          if (-not (Test-Path -LiteralPath $Installer)) {
              throw "Tailscale installer download failed."
          }

          Write-Host "Installing Tailscale..."

          $InstallProcess = Start-Process `
            -FilePath 'msiexec.exe' `
            -ArgumentList @(
              '/i',
              $Installer,
              '/quiet',
              '/norestart'
            ) `
            -Wait `
            -PassThru

          if ($InstallProcess.ExitCode -ne 0) {
              throw "Tailscale installation failed. Exit code: $($InstallProcess.ExitCode)"
          }

          Write-Host "Tailscale installation completed."

          $TimeoutSeconds = 60
          $ElapsedSeconds = 0

          while (-not (Test-Path -LiteralPath $Tailscale)) {

              if ($ElapsedSeconds -ge $TimeoutSeconds) {
                  throw "Tailscale executable was not found."
              }

              Start-Sleep -Seconds 2
              $ElapsedSeconds += 2
          }

          Write-Host "Tailscale executable found."


      # ============================================================
      # 8. Connect Tailscale
      # ============================================================
      - name: 8. Connect Tailscale
        shell: powershell
        env:
          TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY_WIN_1 }}
        run: |
          $ErrorActionPreference = 'Stop'

          $Tailscale = 'C:\Program Files\Tailscale\tailscale.exe'

          if ([string]::IsNullOrWhiteSpace($env:TAILSCALE_AUTHKEY)) {
              throw "GitHub Secret TAILSCALE_AUTHKEY_WIN_1 is not configured."
          }

          Write-Host "Starting Tailscale service..."

          Start-Service `
            -Name 'Tailscale' `
            -ErrorAction SilentlyContinue

          Start-Sleep -Seconds 5

          # GitHub-hosted runners are temporary.
          # Use a unique hostname so multiple historical runners
          # do not fight over the same Tailscale hostname.
          $Hostname = "github-rdp-$env:GITHUB_RUN_ID"

          Write-Host "Tailscale hostname: $Hostname"

          Write-Host "Connecting to Tailscale..."

          # IMPORTANT:
          # Do NOT use --ephemeral.
          #
          # The Tailscale version installed on this Windows runner
          # does not support that flag.
          & $Tailscale up `
            --auth-key="$env:TAILSCALE_AUTHKEY" `
            --hostname="$Hostname" `
            --accept-dns=true `
            --unattended=true

          if ($LASTEXITCODE -ne 0) {
              throw "Tailscale connection failed. Exit code: $LASTEXITCODE"
          }

          Write-Host "Tailscale connection established."

          # Wait for IPv4 address.
          $TailscaleIP = ''

          for ($i = 1; $i -le 15; $i++) {

              $TailscaleIP = (& $Tailscale ip -4 2>$null).Trim()

              if (-not [string]::IsNullOrWhiteSpace($TailscaleIP)) {
                  break
              }

              Start-Sleep -Seconds 2
          }

          if ([string]::IsNullOrWhiteSpace($TailscaleIP)) {
              throw "Tailscale did not provide an IPv4 address."
          }

          Write-Host ""
          Write-Host "==================================================" -ForegroundColor Green
          Write-Host "             RDP SERVER IS READY" -ForegroundColor Green
          Write-Host "==================================================" -ForegroundColor Green
          Write-Host ""
          Write-Host "Tailscale IP : $TailscaleIP" -ForegroundColor Yellow
          Write-Host "Hostname     : $Hostname" -ForegroundColor Yellow
          Write-Host "Username     : CynetAdmin" -ForegroundColor Yellow
          Write-Host ""
          Write-Host "RDP command:" -ForegroundColor Cyan
          Write-Host "mstsc /v:$TailscaleIP" -ForegroundColor Magenta
          Write-Host ""
          Write-Host "==================================================" -ForegroundColor Green
          Write-Host ""

          Write-Host "Tailscale status:"
          & $Tailscale status


      # ============================================================
      # 9. Verify RDP
      # ============================================================
      - name: 9. Verify RDP
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'

          Write-Host "Checking RDP configuration..."

          $RdpDenied = Get-ItemPropertyValue `
            -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
            -Name 'fDenyTSConnections'

          if ($RdpDenied -ne 0) {
              throw "RDP is disabled."
          }

          $TermService = Get-Service `
            -Name 'TermService'

          if ($TermService.Status -ne 'Running') {
              throw "TermService is not running."
          }

          Write-Host "RDP registry: OK"
          Write-Host "TermService: RUNNING"

          Write-Host ""
          Write-Host "Remote Desktop firewall rules:"

          Get-NetFirewallRule `
            -DisplayGroup 'Remote Desktop' `
            -ErrorAction SilentlyContinue |
            Where-Object Enabled -eq 'True' |
            Select-Object DisplayName, Enabled, Profile |
            Format-Table -AutoSize


      # ============================================================
      # 10. Keep RDP Server Alive For 2 Minutes
      # ============================================================
      - name: 10. Keep RDP Server Alive For 2 Minutes
        id: keep_alive
        shell: powershell
        run: |
          $TotalSeconds = 200
          $IntervalSeconds = 10

          Write-Host ""
          Write-Host "==================================================" -ForegroundColor Green
          Write-Host "       RDP SERVER IS NOW AVAILABLE" -ForegroundColor Green
          Write-Host "==================================================" -ForegroundColor Green
          Write-Host ""

          for ($Elapsed = 0; $Elapsed -lt $TotalSeconds; $Elapsed += $IntervalSeconds) {

              $Remaining = $TotalSeconds - $Elapsed

              Write-Host "RDP server running - $Remaining seconds remaining..."

              Start-Sleep -Seconds $IntervalSeconds
          }

          Write-Host ""
          Write-Host "2-minute RDP session completed successfully."


      # ============================================================
      # 11. Save Actual CynetAdmin Desktop
      #
      # IMPORTANT:
      # This step runs ONLY if the RDP session completed successfully.
      #
      # If Tailscale/RDP/setup fails, this step does NOT run.
      # Therefore a failed workflow cannot replace a good backup
      # with an empty Desktop.
      # ============================================================
      - name: 11. Save CynetAdmin Desktop
        if: steps.keep_alive.outcome == 'success'
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'

          $Username = 'CynetAdmin'

          $UserProfile = Join-Path 'C:\Users' $Username
          $Desktop = Join-Path $UserProfile 'Desktop'

          $Workspace = Join-Path $env:GITHUB_WORKSPACE 'workspace'
          $DesktopBackup = Join-Path $Workspace 'Desktop'

          Write-Host ""
          Write-Host "==================================================" -ForegroundColor Cyan
          Write-Host "SAVING CynetAdmin DESKTOP"
          Write-Host "==================================================" -ForegroundColor Cyan
          Write-Host "Source : $Desktop"
          Write-Host "Target : $DesktopBackup"
          Write-Host ""

          if (-not (Test-Path -LiteralPath $Desktop)) {
              throw "CynetAdmin Desktop does not exist."
          }

          if (-not (Test-Path -LiteralPath $DesktopBackup)) {
              New-Item `
                -ItemType Directory `
                -Path $DesktopBackup `
                -Force | Out-Null
          }

          Write-Host "Synchronizing Desktop..."

          robocopy `
            $Desktop `
            $DesktopBackup `
            /MIR `
            /COPY:DAT `
            /DCOPY:DAT `
            /R:2 `
            /W:1 `
            /XJ

          # Robocopy 0-7 = successful/non-fatal.
          if ($LASTEXITCODE -gt 7) {
              throw "Desktop backup failed. Robocopy exit code: $LASTEXITCODE"
          }

          Write-Host ""
          Write-Host "Desktop saved successfully."

          Write-Host ""
          Write-Host "FILES STORED IN PERSISTENT BACKUP:"
          Write-Host "--------------------------------------------------"

          Get-ChildItem `
            -LiteralPath $DesktopBackup `
            -Force `
            -Recurse `
            -ErrorAction SilentlyContinue |
            Select-Object FullName, Length, LastWriteTime |
            Format-Table -AutoSize

          Write-Host "--------------------------------------------------"


      # ============================================================
      # 12. Save Persistent Workspace To GitHub Cache
      #
      # ONLY save when the Desktop was successfully synchronized.
      #
      # This is critical.
      #
      # We deliberately DO NOT use:
      #
      #   if: always()
      #
      # because a failed setup could otherwise save an empty/broken
      # workspace as the newest cache.
      # ============================================================
      - name: 12. Save Workspace For Next Run
        if: steps.keep_alive.outcome == 'success'
        uses: actions/cache/save@v5
        with:
          path: ${{ github.workspace }}\workspace

          # Unique immutable cache key.
          key: cynet-rdp-workspace-${{ runner.os }}-${{ github.run_id }}


      # ============================================================
      # 13. Final Persistence Verification
      # ============================================================
      - name: 13. Verify Persistent Backup
        if: steps.keep_alive.outcome == 'success'
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'

          $Workspace = Join-Path $env:GITHUB_WORKSPACE 'workspace'
          $DesktopBackup = Join-Path $Workspace 'Desktop'

          Write-Host ""
          Write-Host "==================================================" -ForegroundColor Green
          Write-Host "PERSISTENCE VERIFICATION"
          Write-Host "==================================================" -ForegroundColor Green

          if (-not (Test-Path -LiteralPath $DesktopBackup)) {
              throw "Desktop backup directory does not exist."
          }

          $Files = Get-ChildItem `
            -LiteralPath $DesktopBackup `
            -Force `
            -Recurse `
            -File `
            -ErrorAction SilentlyContinue

          Write-Host ""
          Write-Host "Persisted file count: $($Files.Count)"
          Write-Host ""

          if ($Files.Count -eq 0) {
              Write-Host "WARNING: Desktop backup contains zero files."
              Write-Host "This may be correct if the Desktop was intentionally empty."
          }
          else {
              Write-Host "Persisted files:"
              Write-Host "--------------------------------------------------"

              $Files |
                Select-Object FullName, Length, LastWriteTime |
                Format-Table -AutoSize

              Write-Host "--------------------------------------------------"
          }

          Write-Host ""
          Write-Host "Persistent workspace is ready for the next run."
